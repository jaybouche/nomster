<% @places.each do |place| %>
<div class=".will_paginate col-1">
  <%= will_paginate @places, previous_label: "<", next_label: h(">") %>
 <div class="pages col-10 offset-1">
</div>
</div>
  <div class="booyah-box col-10 offset-1">
    <h1><%= place.name %></h1>
    <i><%= place.address %></i><br /><br />
    <p><%= place.description %></p><br />
  </div>
<% end %>